import java.util.Scanner;
import java.io.*;

void setup()//Prexisting method in processing. It will be used for initial load.
{
  size(1024,512);
}

void draw()//Prexisting method in processing. It will be used as an updater.
{
  //time based updater method
  
  
}

void keyPressed()//Prexisting method in processing. It will be used for controls.
{
  //movement
  if(true)
  {
  if(key == 'a'){}
  else if(key == 'd'){}
  else if(key == 'w'){}
  else if(key == 's'){}
  //other controls
  else if(key == 'f'){}
  //numbers
  else if(key == '1'){}
  
  }
}
